/**
 * Convenience re-export of getDisplayMode so consumers can import from utils.
 */
export { getDisplayMode } from "../types/DisplayMode.ts";
export type { DisplayMode } from "../types/DisplayMode.ts";
